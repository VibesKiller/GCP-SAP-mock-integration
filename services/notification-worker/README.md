# notification-worker

Planned responsibility:

- consume canonical business events for optional outbound side effects
- model fan-out patterns without coupling them to the critical ingestion path
- remain optional so the core platform stays focused and finishable
