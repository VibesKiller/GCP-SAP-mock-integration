# Artifact Registry Module

Creates the Docker repository used by the platform build and deployment pipeline.

The repository is intentionally shared by the four application services in each environment to keep the portfolio example easy to reason about.
