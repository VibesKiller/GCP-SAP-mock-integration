# Helm Baseline

This chart is the initial packaging boundary for the platform on GKE. The first iteration only establishes shared values, naming and service account conventions. Workload templates for the individual services will be added once the Go applications are implemented.
